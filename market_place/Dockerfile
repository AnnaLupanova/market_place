FROM python:3.8
WORKDIR /app
ADD . /app
COPY requirements.txt .
RUN pip install -r requirements.txt
ARG DJANGO_SECRET_KEY
ENV DJANGO_SECRET_KEY $(set DJANGO_SECRET_KEY)
ENV EMAIL_HOST_USER_ $(set EMAIL_HOST_USER_)
ENV EMAIL_HOST_PASSWORD_ $(set EMAIL_HOST_PASSWORD_)


